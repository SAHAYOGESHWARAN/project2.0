<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <!-- Correct paths for static assets -->
    <link rel="stylesheet" href="/static/css/styles.css"> <!-- Corrected static path -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"> <!-- FontAwesome CDN -->
</head>
<body>
    <!-- Include the sidebar with the activePage variable to highlight the active link -->
    <%- include('partials/sidebar', { activePage: 'users' }) %>

    <div class="main-content">
        <h1>Users Management</h1>

        <div class="user-actions">
            <input type="text" id="search" placeholder="Search users..." class="search-bar">
            <button id="add-user-btn" class="add-user-btn">Add User</button>
        </div>

        <!-- Add User Form -->
        <div id="add-user-form" class="hidden">
            <h2>Add New User</h2>
            <form action="/users/add" method="POST">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required>

                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>

                <label for="role">Role:</label>
                <select id="role" name="role">
                    <option value="Admin">Admin</option>
                    <option value="User">User</option>
                </select>

                <button type="submit" class="submit-btn">Add User</button>
                <button type="button" id="cancel-btn" class="cancel-btn">Cancel</button>
            </form>
        </div>

        <table class="user-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="user-list">
                <!-- User rows will be dynamically inserted here -->
            </tbody>
        </table>

        <div class="pagination">
            <button class="prev-btn">Previous</button>
            <span class="page-info">Page 1 of 10</span>
            <button class="next-btn">Next</button>
        </div>
    </div>

    <!-- Correct JS paths -->
    <script src="/static/js/sidebar-toggle.js"></script>
    <script src="/static/js/users.js"></script> <!-- Users management logic -->
</body>
</html>
